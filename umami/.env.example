# Umami Analytics Environment Variables

# Database connection string (PostgreSQL)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://umami:umami@localhost:5432/umami
DATABASE_TYPE=postgresql

# Application secret (generate with: openssl rand -hex 32)
# This is used for session encryption and JWT signing
APP_SECRET=change-me-to-random-string

# Server port (optional, defaults to 3000)
PORT=3000

# Hostname (optional, for Railway deployment)
# HOSTNAME=0.0.0.0

# Disable telemetry (optional, recommended for privacy)
DISABLE_TELEMETRY=1

# Client IP header (optional, for Cloudflare proxy)
CLIENT_IP_HEADER=CF-Connecting-IP
